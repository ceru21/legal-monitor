# Copy to .env and fill in your values. Never commit .env to version control.

# -- Desarrollo local (coherente con docker-compose.yml defaults) --
DATABASE_URL=postgresql://legal_monitor:changeme@localhost:5432/legal_monitor
POSTGRES_PASSWORD=changeme

# Optional: enforce SSL for the DB connection (disable | allow | require | verify-full)
# DB_SSLMODE=require
